{
  "name": "landskrona-inspektionsrapporter-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "node --env-file=.env.local dev.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/app.js && node --check src/reports.js && node --check src/storage.js",
    "cleanup": "node --env-file=.env.local scripts/cleanup.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "@vercel/blob": "2.8.0",
    "docxtemplater": "3.69.3",
    "express": "^5.1.0",
    "pizzip": "^3.2.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@xmldom/xmldom": "^0.9.12"
  }
}
